<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Sanity Check</title>
</head>
<body>
    <h1>Sanity Check</h1>
    <p>This page will fail to work properly if VCWD is not built.</p>
    <p>Below should be the result of the sanityCheck function in VCWD:</p>
    <script>
        document.addEventListener('DOMContentLoaded', () => {
            const result = VCWD.sanityCheck(1);
            console.log(result);
            document.body.innerHTML += `<p>${result}</p>`;
        });
    </script>
</body>
</html>